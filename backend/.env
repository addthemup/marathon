# Django settings module for development
DJANGO_SETTINGS_MODULE=backend.settings

# Django secret key (ensure this is kept secure, but can be less restrictive in development)
DJANGO_SECRET_KEY=ghez6pqq(772rw0ohjgt_7wyghmuq$&mija*(!y+tn&r=$l_23


# Database configuration for development
DJANGO_DB_NAME=marathon_dev
DJANGO_DB_USER=awc
DJANGO_DB_PASSWORD=dev_password
DJANGO_DB_HOST=localhost
DJANGO_DB_PORT=5432

# Environment
ENVIRONMENT=development
DEBUG=True

# CORS and CSRF trusted origins for development
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# Additional settings
ALLOWED_HOSTS=localhost,127.0.0.1
